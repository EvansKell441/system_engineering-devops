#!/usr/bin/env bash
# A Bash script that prints "I am invincible!!!" when receiving SIGTERM

# Trap SIGTERM and print the required message immediately
trap 'echo I am invincible!!!' SIGTERM

# Infinite loop (no sleep)
while true; do
    :
done
