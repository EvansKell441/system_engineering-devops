#!/usr/bin/env bash
# A Bash script that continuously displays "To infinity and beyond" every 2 seconds
# Prints "I am invincible!!!" immediately when receiving a SIGTERM signal

# Trap SIGTERM and print the message immediately
trap 'echo "I am invincible!!!"' SIGTERM

while true
do
  echo "To infinity and beyond"
  sleep 2
done
