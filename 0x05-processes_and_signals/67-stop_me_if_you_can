#!/usr/bin/env bash
# A Bash script that sends SIGTERM to the 7-highlander process

# Find the PID of the 7-highlander script
HIGHLANDER_PID=$(pgrep -f 7-highlander)

# Send SIGTERM if process exists
if [ -n "$HIGHLANDER_PID" ]; then
    kill -TERM "$HIGHLANDER_PID"
fi
